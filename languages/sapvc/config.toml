name = "SAP VC"
grammar = "sapvc"
line_comments = ["*"]
tab_size = 2

# LSP binding happens via extension.toml [language_servers.sapvc-lsp]
# languages = ["SAP VC"] + the Rust shell's language_server_command (M3-4).
